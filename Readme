##### RAG Chatbot using Streamlit


### Set up

1. create a python virtual environment
2. cd to the environment
3. run `pip install requirements.txt` to install libarie

To test excel_chatbot.py

1. run `streamlit run excel_chatbot.py` and visit http://localhost:8501/  to interact with the app
  - provide your api-key 
  - upload excel files
  - chat with your documents

